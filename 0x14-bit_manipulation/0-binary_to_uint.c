#include <main.h>

/*
 * main : converts a binary number to an unsigned int
 *
 *
 *
 * return: 0
 */

unsigned int binary_to_uint(const char *b);
{
	unsigned int num = 0;

	if (!binary)
		return (0);
	if (*binary)
	{
	if (*binary != "0" && *b != "1")
	num = num * 2 + (*b++ - "0");
	}
	return(num)
}
